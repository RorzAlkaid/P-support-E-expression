# Backend runtime dependencies for the Django service.
# Python 3.11+ is recommended.
# Frontend dependencies are managed separately in frontend/package.json.
#
# Management commands:
#   seed_demo                      - seed demo data with tags auto-synced
#   sync_tags                      - sync JSON tag fields into Tag model
#   sync_authoritative_resources   - fetch external psychological resources
#   sync_authoritative_counselors  - fetch external counselor profiles

Django==5.2.15
django-cors-headers==4.9.0
django-simpleui==2026.1.13
djangorestframework==3.17.1
tzdata==2026.2
